# Pinned for reproducible deploys. Deliberately lean: the whole app runs on a free
# Streamlit Community Cloud dyno, which means no PyTorch.
streamlit==1.59.0
pydantic==2.9.2
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1

# Document ingestion
pymupdf==1.24.14           # PDF text + block geometry (column-order repair)
python-docx==1.1.2         # DOCX body-order walk, merged-cell handling

# Retrieval. fastembed is ONNX-based: same bge-small weights as sentence-transformers
# but ~50MB instead of ~2GB, which is what keeps this deployable on the free tier.
fastembed==0.4.2
faiss-cpu==1.9.0
rapidfuzz==3.10.1

# LLM
anthropic==0.52.2
python-dotenv==1.0.1

# LangChain multi-agent orchestrator (opt-in, additive to the default Python pipeline
# above -- see src/millennium/langchain_agents.py for why both exist). No heavy
# transitive deps (no torch); verified against this project's free-tier deploy budget.
langchain==0.3.7
langchain-core==0.3.63
langchain-anthropic==0.3.15

# Original-document view on the Candidate page: PDFs render natively via st.pdf;
# DOCX has no browser-native renderer, so mammoth converts to HTML once (cached).
# Pure Python, no LibreOffice/system dependency -- safe for the free deploy tier.
mammoth==1.8.0
streamlit-pdf==2.0.1        # backs st.pdf(); no further dependencies
