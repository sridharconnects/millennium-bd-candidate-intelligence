# Streamlit Community Cloud: paste this into Settings -> Secrets.
# The deployed app runs in DEMO_MODE and needs no key at all; include one only if you
# want live re-parsing from the deployed instance.
ANTHROPIC_API_KEY = "sk-ant-..."
DEMO_MODE = "1"
